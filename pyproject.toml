[project]
name = "servicenow-mcp"
version = "0.1.0"
description = "MCP server for ServiceNow incident management"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
servicenow-mcp = "server:mcp.run"
